# AI 투두 생성 규칙 (aiprompt.md)

> 이 문서는 slowgoes의 **AI 투두 자동생성** 품질 규칙의 단일 기준이다.
> 구현: `src/lib/ai/analyze.ts`의 `generateTodoSuggestions` 프롬프트가 이 규칙을 따른다.
> **규칙을 바꾸면 이 문서와 프롬프트를 함께 수정한다.**

## 목적

유저가 "지금 바로 시작할 수 있는" 질 좋은 투두 3개를 제안한다.
완벽한 계획이 아니라 **70점짜리 행동** — 어설퍼도 오늘 착수할 수 있는 행동 — 을 지향한다.
(앱의 목적: 하나의 버킷에 집중해 행동력·실천력을 높이는 것 — CLAUDE.md Philosophy)

## 컨텍스트 소스 (프롬프트에 주입하는 정보)

| 소스 | 역할 | 없을 때 |
|---|---|---|
| ① **버킷 타이틀 + 삶의 영역** | 무엇을 위한 행동인지 (필수) | — |
| ② **지향점 3단** (언젠가 / 올해안 / 해당 달) | 방향의 근거. **투두는 "해당 달" 발걸음에 직결**되고, 언젠가·올해안은 톤을 잡는 배경 | 있는 것만 사용 |
| ③ **MBTI** (personality_type) | 행동 스타일 조정 — I형: 혼자 시작 가능한 행동 우선 / E형: 사람과 연결되는 행동 허용 / J형: 계획·정리형 / P형: 즉흥·탐색형 | 생략 |
| ④ **나이** (life_clock_age) | 인생 단계 맥락 (표현·난이도 조정) | 생략 |
| ⑤ **최근 일기 3건 발췌** (plain_text 앞 200자) | 현재 관심사·감정 상태 반영 — 일기에 드러난 상황이 있으면 1개는 그것과 연결 | 생략 |
| ⑥ **기존 투두 제목 전체** | **중복 금지** 목록 | 필수(빈 목록 가능) |
| ⑦ **기준 날짜** (캘린더 선택 날짜) | "그 날" 실행 가능한 행동인지 판단 | 오늘 |

## 품질 규칙 (7가지)

1. **구체적 행동 동사로 시작** — "~에 대해 알아보기"(추상) ✕ → "~ 검색해서 후보 3개 적기"(구체) ○
2. **30~60분 실행 단위** — 하루 안에, 한 번에 끝낼 수 있는 크기
3. **70점 지향** — 완벽한 준비·조사보다 어설픈 착수를 우선하는 문장
4. **완료 판정 가능** — 개수·시간·산출물이 문장에 있어 체크 가능할 것 (예: "3곳", "10분", "1개 작성")
5. **버킷·지향점 직결** — 해당 달 발걸음을 실제로 전진시키는 행동만
6. **일기 반영(있으면)** — 최근 일기의 관심사/막힘과 연결되는 항목 1개 포함
7. **접근 다양화** — 3개가 서로 다른 유형이 되게: (조사·준비형) / (실행·산출형) / (사람·환경 연결형) 중 조합

## 출력 형식

- **정확히 3개**, 한국어, 각 40자 이내 권장
- JSON만 응답:

```json
{ "todos": [ { "title": "..." }, { "title": "..." }, { "title": "..." } ] }
```

## 금지

- 기존 투두 제목과 의미가 겹치는 항목
- "매일 ~하기" 같은 반복 전제 문장 (반복 설정은 유저가 [반복] 버튼으로 결정)
- 동기부여 문구·이모지·설명 — 행동 문장만

---

# 주간 목표 생성 (generateWeeklyGoals)

> 주간 시트 맨 위의 **주간 목표** 기록에서 [AI 목표] 버튼이 쓰는 프롬프트 규칙.
> 구현: `src/lib/ai/analyze.ts`의 `generateWeeklyGoals` → `generateWeeklyGoalsAction`.
> **규칙을 바꾸면 이 문서와 프롬프트를 함께 수정한다.**

## 목적

투두가 "오늘 착수할 행동"이라면, 주간 목표는 **한 주가 끝났을 때 남아 있을 결과**다.
유저는 제안 중 원하는 것만 골라 일기 본문에 **체크박스 목록**으로 넣고, 한 주 동안 체크한다.

## 컨텍스트 소스

투두 생성과 같다(①~⑥). 다만 ⑦ 기준 날짜 대신 **대상 주 범위**("8.2 ~ 8.8")를 넣는다.

## 품질 규칙

1. **한 주 안에 완료 가능한 크기** — 한 달짜리 과제 ✕
2. **완료 판정 가능** — 개수·횟수·산출물이 문장에 있어 주말에 판정할 수 있을 것
3. **구체적 행동 동사로 시작** — 추상 표현 ✕
4. **70점 지향** — 완벽한 준비보다 어설픈 착수
5. **유형 다양화** — 조사·준비형 / 실행·산출형 / 사람·환경 연결형 / 습관·반복형
6. **분량 절제** — 이번 주에 다 해도 벅차지 않을 정도

## 출력 형식

- **4~5개**, 한국어, 각 40자 이내
- JSON만 응답:

```json
{ "goals": [ { "title": "..." }, { "title": "..." }, { "title": "..." }, { "title": "..." } ] }
```

---

# 일기 organize 모드 (diary-prompts.ts)

> 일기 상세의 **organize**(AI) 시트가 쓰는 프롬프트 규칙.
> 구현: `src/lib/ai/diary-prompts.ts`의 `DIARY_ORGANIZE_MODES` + `buildDiaryPrompt`.
> 서버 호출: `analyzeDiary`(`src/lib/ai/diary-analyze.ts`) → `analyzeDiaryAction`.
> **규칙을 바꾸면 이 문서와 `diary-prompts.ts`를 함께 수정한다.**

## 목적

유저가 **글을 더 잘 쓰도록** 돕는다. 요약뿐 아니라 구조·문맥·어휘·논리 관점의 피드백을 제공한다.
응답은 저장하지 않는 읽기 전용이며, 유저가 [comment]로 일기 아래에 코멘트로 붙일 수 있다.
코멘트 제목 = 누른 버튼의 `label`. **코멘트는 일기 본문과 분리 저장**되어 재-organize 시 AI 입력에 포함되지 않는다.

## 모드 (label = 버튼명 = 코멘트 제목)

| id | label | 지시 요지 |
|---|---|---|
| `summary` | 요약하기 | 3~4문장 부드러운 요약(핵심 사건·감정 흐름) |
| `essay` | 논술 관점에서 평가하기 | 서·본·결 구조, 주제 명료성, 주장-근거 연결 등 구조적 조언 2~3 |
| `flow` | 문맥 어색한 부분 찾기 | 문장 간 흐름이 끊기는 지점 원문 인용 + 대안 제시 |
| `vocabulary` | 어휘력 높이기 | 반복·평범 표현 → 대체 표현 2~3개씩 (톤 유지) |
| `logic` | 논리적 빈틈 찾기 | 기승전결 중 빠진 단계·논리 비약 지적 + 보완 방향 |

- 자유 질문(textarea)도 지원 — 이때 코멘트 제목은 질문 텍스트(앞 60자).

## 공통 규칙 (buildDiaryPrompt 래퍼)

1. 한국어, 담백하게 — 과장된 위로·상투구·이모지 금지
2. 일기(및 선택 부분)에 실제로 있는 내용에 근거 — 지어내기 금지
3. 지적 시 원문을 짧게 인용해 위치를 분명히 + 개선 방향 동반
4. 마크다운 표제(#) 없이 자연스러운 문단, 8문장 이내
5. 드래그로 선택한 부분이 있으면 그 맥락 우선
