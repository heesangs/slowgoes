// 화면 폭 — "무엇이 담기는가"로 정한다.
//
// 대시보드·회고·일기는 카드와 리스트가 담기므로 넓고(max-w-2xl),
// 로그인·회원가입은 입력 필드 2~3개뿐이라 같은 폭을 주면 한 줄이 과하게 길어져
// 읽기 어렵다. 그래서 폼만 좁게 둔다 — 임의로 갈린 게 아니라 의도된 두 값이다.

/** 대시보드 기준 폭. 콘텐츠(카드·리스트·온보딩 선택지)가 담기는 화면 */
export const CONTENT_WIDTH = "max-w-2xl";

/** 인증 폼 전용 폭. 입력 필드 위주 화면 */
export const FORM_WIDTH = "max-w-sm";
