// 공유 UI 스타일 상수 — 여러 컴포넌트가 같아야 하는 값만 둔다.

/**
 * 상단 고정 서피스(헤더·버킷 바)의 그림자.
 *
 * 스크롤할 때 "한 층 위에 떠 있다"만 전하면 되므로 Figma 그림자 사다리의 맨 아래.
 * 다크에서는 그림자가 거의 보이지 않으므로 기존 `border-b`와 함께 쓰는 것을 전제로 한다.
 */
export const SURFACE_SHADOW = "shadow-normal";
