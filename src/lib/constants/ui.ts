// 공유 UI 스타일 상수 — 여러 컴포넌트가 같아야 하는 값만 둔다.

/**
 * 상단 고정 서피스(헤더·버킷 바)의 그림자.
 *
 * 스크롤할 때 "한 층 위에 떠 있다"만 전하면 되므로 아주 약하게. 다크에서는 그림자가
 * 거의 보이지 않으므로 기존 `border-b`와 함께 쓰는 것을 전제로 한다.
 */
export const SURFACE_SHADOW = "shadow-[0_1px_2px_-1px_rgb(0_0_0/0.06),0_4px_10px_-6px_rgb(0_0_0/0.10)]";
